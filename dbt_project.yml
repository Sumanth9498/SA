name: 'tipico_events'
version: '1.0.0'
config-version: 2

models:
  tipico_events:
    +materialized: table
  staging:
    +materialized: view
  raw_tipico_events:
    +column_types:
      data: STRING
  marts:
    +materialized: table
    core:
      +schema: tipico_marts

model-paths: ["models"]

packages:
  - package: dbt-labs/redshift
    version: 1.5.8
